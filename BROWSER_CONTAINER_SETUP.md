# browser_container 스크린샷 도구 준비 가이드

## 먼저 확인할 점
`browser_container`는 일반 npm 패키지처럼 프로젝트 내부에 설치하는 도구가 아니라,
**에이전트 실행 환경(클라이언트/플랫폼)에서 제공되는 내장 툴**입니다.

즉, 저장소 안에서 `npm install`로 설치하는 방식이 아닙니다.

---

## 사용 가능하게 하려면

### 1) 에이전트 실행 환경에서 browser tool 활성화
- Codex/에이전트 런타임 설정에서 `browser_container`(또는 browser tool)를 켭니다.
- 세션 시작 시 툴 목록에 browser 계열 도구가 노출되는지 확인합니다.

### 2) GUI/헤드리스 브라우저 실행 가능한 런타임 사용
- CI나 서버 환경이라면 headless Chrome/Chromium 실행이 가능한 이미지 필요
- 샌드박스 정책에서 브라우저 프로세스 실행이 차단되지 않아야 함

### 3) 아티팩트 저장 경로 권한 확인
- 스크린샷 저장 경로에 쓰기 권한 필요
- 세션 종료 후 아티팩트 접근 가능한 경로인지 확인

---

## 최소 체크리스트
- [ ] 세션 툴 목록에 browser_container가 보임
- [ ] 브라우저 실행 권한 허용됨
- [ ] 네트워크/로컬 파일 접근 정책이 테스트 시나리오와 맞음
- [ ] 스크린샷 아티팩트 경로에 write 가능

---

## 문제 발생 시 빠른 점검
1. "tool not found" -> 런타임에서 browser tool 미활성화
2. "launch failed" -> 브라우저 바이너리/권한/샌드박스 문제
3. "artifact write failed" -> 출력 경로 권한 문제
4. "blank screenshot" -> 페이지 로드 대기/렌더 완료 타이밍 문제

---

## 이 저장소에서의 의미
현재 저장소(`nagi-memo`)에는 코드 변경만으로 browser_container를 설치/활성화할 수 없습니다.
도구는 **실행 환경 레벨 설정**에서 켜주셔야 하며,
활성화되면 이후 턴부터 제가 UI 변경 시 스크린샷을 첨부할 수 있습니다.
